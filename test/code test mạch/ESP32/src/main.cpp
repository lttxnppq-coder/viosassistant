/*
 * ============================================================
 * TEST 2 - ESP32 NodeMCU LuaNode32 (38 pin)
 * File này chỉ là placeholder để giữ cấu trúc project.
 * Code ESP32 sẽ được viết sau khi TEST 1 PASS.
 * Cấu hình đã xác định từ tài liệu:
 *   CAN  : TX = GPIO5, RX = GPIO4, 500 kbps, Standard 11-bit
 *   UART : TX = GPIO1, RX = GPIO3, 9600 baud
 * ============================================================
 */
#include <Arduino.h>

void setup() {
}

void loop() {
}
